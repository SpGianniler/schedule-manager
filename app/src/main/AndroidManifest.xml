<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schedule_manager">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Schedulemanager">
        <activity android:name=".AdminShowEmployeesActivity"></activity>
        <activity android:name=".UserProfileSettings" />
        <activity android:name=".AdminWelcomeActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".UserWelcomeActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".UserLoginActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>